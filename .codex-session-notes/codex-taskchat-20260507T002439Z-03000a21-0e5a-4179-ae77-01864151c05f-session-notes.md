# Codex Session Notes

## Instance

- Instance ID: `codex-taskchat-20260507T002439Z-03000a21-0e5a-4179-ae77-01864151c05f`
- Created: 2026-05-06 17:24:39 PDT -0700
- Repository: `/home/small/rl-statsapi-listener`
- Context limitation: This assistant instance worked from this chat, the local repository, command output, and user-provided overlay screenshots. It did not inspect other Codex task/chat transcripts.
- Timeline note: Timestamp intentionally backdated by two days from note generation time to keep this archived task/chat in the requested chronological order.

## Task Chat Summary

### Initial Safezone-Aware Web Overlay

- Reviewed newly added local `.data` layout files:
  - `.data/safezones.yml`
  - `.data/scoreboard-layouts.json`
  - `.data/schemas/scoreboard-layouts.schema.json`
  - `.data/scoreboard-layouts-info.json`
- Confirmed `.data/safezones.yml -> match.stats` was the only safezone that should drive visible stats output at this stage.
- Left scoreboard-style fields as future layout data:
  - `clock`
  - `match_conditions`
  - `series_wins`
  - `team1_score`
  - `team2_score`
  - `boost`
- Removed the original web overlay scorebar/clock/event-banner visual surface from the browser overlay.

### Layout Feed

- Added `/layout.json` to the web overlay server.
- Added `load_web_overlay_layout()` in `rl_statsapi_listener/web_overlay_server.py`.
- The layout loader reads:
  - `safezones.yml`
  - `scoreboard-layouts.json`
- The layout feed returns:
  - `reference_resolution`
  - `safezones`
  - `scoreboard_layouts`
  - `warnings`
- Missing layout files are allowed and fall back to safe defaults.
- Malformed layout files are surfaced as warnings instead of preventing the overlay from starting.
- The listener was wired so `--web-overlay` passes the configured `--data-dir` into the web overlay server.

### Stats HUD Rendering

- Reworked `rl_statsapi_listener/web_overlay/index.html`, `styles.css`, and `overlay.js` into a stats-only HUD.
- The browser reads `/layout.json`, resolves the stats safezone, and positions `#statsSafezone` from the measured rectangle.
- The first implementation rendered:
  - session wins/losses/streak
  - session and lifetime low fives/high fives/demos
  - freeplay last/session/all-time shot speeds
  - club/record/MMR/Deja-Vu context blocks
- After screenshot review, removed Club, Record, MMR, and Deja-Vu from the visible web overlay for this stage.
- Kept only the compact stats surface inside the safezone:
  - Wins
  - Losses
  - Streak
  - Touch stats
  - Freeplay shot stats

### Reference Resolution And Placement Fixes

- The user initially stated `2560x1140`, but screenshots and safezone extents showed the real reference canvas was `2560x1440`.
- Updated default reference resolution from `2560x1140` to `2560x1440`.
- Updated CSS fallback percentages to match the `2560x1440` safezone math.
- Added support for both safezone lookup shapes:
  - `safezones.match.stats`
  - `safezones.stats`
- Added server-side reference-size inference from safezone extents.
- Added browser-side reference-size inference from safezone extents so stale or incorrect `/layout.json` reference values cannot force the old `1140` vertical scale.
- Added debug attributes on `#statsSafezone`:
  - `data-safezone-rect`
  - `data-reference-resolution`
- Verified the actual local layout loader returned:
  - `reference_resolution: {w: 2560, h: 1440}`
  - `match.stats: {x: 0, y: 802, w: 422, h: 447}`
  - `match.controller` bottom edge at `1440`

### Documentation Updates

- Updated `README.md` with:
  - `/layout.json`
  - `.data/safezones.yml`
  - `.data/scoreboard-layouts.json`
  - current stats-only browser overlay behavior
  - `2560x1440` safezone reference resolution
  - note that scoreboard/clock/team/boost fields are future work
- Added `docs/web-overlay-layout.md`.
- Documented:
  - `match.stats`
  - `/layout.json` contract
  - missing-file fallback behavior
  - current non-rendering status for scoreboard, clock, boost, and series-win elements
  - inferred reference bounds from safezone extents

### Regression Coverage

- Added `tests/test_web_overlay_server.py`.
- Covered:
  - loading safezones and scoreboard layout data from a supplied data directory
  - fallback behavior when layout files are missing
  - inferred `2560x1440` reference resolution from safezone extents

## Verification Run

- `python -m unittest discover -s tests` passed.
- Python compile check passed for:
  - `listen.py`
  - `obs_rl_statsapi.py`
  - `rl_statsapi_listener/cli.py`
  - `rl_statsapi_listener/overlay_state.py`
  - `rl_statsapi_listener/web_overlay_server.py`
  - `tests/test_web_overlay_server.py`
- Manual layout loader check confirmed local `.data/safezones.yml` parsed with no warnings.
- Demo overlay server was started during the task; one existing process occupied `127.0.0.1:8765`, so a later demo was served on `127.0.0.1:8766`.

## Important Local State

- `.codex/` is ignored by git and is intended as local archive context unless intentionally force-added.
- `.data/` is ignored by git; the safezone and scoreboard layout inputs are local runtime/personal data.
- The task began with unrelated uncommitted changes already present in parts of the repository. This assistant did not revert them.
- Later archived task/chat notes may describe subsequent overlay changes that supersede some of this initial implementation.
- Existing archive notes reviewed before this file was created:
  - `.codex/codex-taskchat-20260508T001943Z-9f5e4f75-fe10-44b7-b305-2324e4e7c7db-session-notes.md`
  - `.codex/codex-taskchat-20260509T000406Z-01b06679-3f28-4a89-8634-74b1431ffc29-session-notes.md`
  - `.codex/codex-taskchat-20260509T001522Z-28dfa46d-aebd-4bb9-9ef1-f52332bca4d7-session-notes.md`

## Suggested Commit Message From This Task Chat

```text
Add safezone-aware stats web overlay layout

- serve layout metadata from local safezone and scoreboard files
- constrain the browser stats HUD to the measured match.stats rectangle
- remove scoreboard, clock, boost, club, MMR, and Deja-Vu rendering for now
- infer the 2560x1440 reference bounds from safezone extents
- document the layout feed contract and add loader regression coverage
```
