# Codex Session Notes

## Instance

- Instance ID: `codex-taskchat-20260502T183500Z-095f40dc-25b4-41dc-9985-00291e615245`
- Created: 2026-05-02 11:35:00 PDT -0700
- Repository: `/home/small/rl-statsapi-listener`
- Chronological next archived task/chat notes: `.codex/codex-taskchat-20260503T030101Z-305623c3-bb6a-4ac7-827f-b60945c1d788-session-notes.md`
- Context limitation: This assistant instance worked from this chat, the local repository, command output, and the `.codex/` archive files explicitly requested in this chat. It did not inspect external Codex task/chat transcripts.
- Timeline note: Timestamp intentionally uses the user's requested prior runtime, `2026-05-02T11:35:00-0700`, so this archive fits the existing local task/chat timeline.

## Task Chat Summary

### Browser Overlay Direction

- Shifted the overlay approach away from OBS-projected transparent windows and toward a local browser overlay that can be consumed by OBS Browser Source or a native transparent host.
- Added a zero-dependency local HTTP server in `rl_statsapi_listener/web_overlay_server.py`.
- Added browser overlay assets under `rl_statsapi_listener/web_overlay/`:
  - `index.html`
  - `styles.css`
  - `overlay.js`
  - package marker `__init__.py`
- Added package-data configuration in `pyproject.toml` so the HTML/CSS/JS assets are included with the Python package.
- Added `--web-overlay`, `--web-host`, and `--web-port` CLI flags.
- The default browser overlay URL is `http://127.0.0.1:8765/`.
- The live structured state endpoint is `http://127.0.0.1:8765/state.json`.

### Overlay State And Data Contract

- Expanded `OverlayStatsTracker` with structured browser-overlay state via `get_overlay_state()`.
- Added live fields for:
  - clock
  - overtime
  - blue/orange scores
  - last event name
  - event banner
  - current match metadata
  - session wins/losses/streak
  - session low fives/high fives/demos
  - lifetime low fives/high fives/demos
  - freeplay shot speed summaries
  - club display and club record
  - recent playlist MMR display
  - structured Deja-Vu rows
- Added `overlay_state.json` alongside the OBS text output files when `--obs-dir` is provided.
- Added basic live text outputs for clock, scores, event name, and event banner to the SQLite-backed text-file path.
- Preserved the existing OBS text-file workflow while adding the browser state feed.

### Web Server Threading Fix

- Found that `/state.json` could return HTTP 500 because the web server read SQLite-backed state from a background HTTP thread.
- Updated the SQLite connection to use `check_same_thread=False`.
- The listener and HTTP snapshot reads are protected by the existing tracker lock in `cli.py`.
- Added a regression test proving overlay state can be read from a separate web-server-style thread.
- Updated the browser JavaScript to show a visible "Overlay data feed unavailable" banner if `/state.json` fails instead of silently staying on default values.

### Windows Transparent WebView Host

- Added a Windows-only .NET 8 WPF/WebView2 host under `integrations/windows-webview-host/`.
- Host files added:
  - `RlStatsApiOverlay.Host.csproj`
  - `Program.cs`
  - `App.cs`
  - `OverlayOptions.cs`
  - `OverlayWindow.cs`
  - `NativeMethods.cs`
  - `Start-OverlayHost.ps1`
  - `app.manifest`
  - `README.md`
- The host uses `WebView2CompositionControl` with transparent background.
- The native window is configured as:
  - borderless
  - transparent
  - topmost by default
  - no-activate
  - click-through by default
  - tool-window style
- Added global hotkeys:
  - `Ctrl+Shift+F10`: toggle click-through/interactive mode
  - `Ctrl+Shift+F11`: reload WebView
  - `Ctrl+Shift+F9`: exit host
- Added monitor selection, zoom, custom bounds, debug flags, and taskbar/devtools options.
- Added `Start-OverlayHost.ps1` to wrap `dotnet run`, validate Windows .NET 8 SDK availability, and pass arguments consistently.
- Added bounds validation in the PowerShell wrapper so custom bounds require `-X`, `-Y`, `-Width`, and `-Height`.

### WSL And Windows Runtime Guidance

- Clarified that the Python listener may run in WSL, but the transparent WebView host must run as a Windows process.
- Added WSL launch command using Windows PowerShell:
  - `powershell.exe -NoProfile -ExecutionPolicy Bypass -File "$(wslpath -w integrations/windows-webview-host/Start-OverlayHost.ps1)" -Url http://127.0.0.1:8765/ -Monitor 1`
- Confirmed Windows PowerShell was callable from WSL.
- Confirmed Windows initially had .NET 7 SDKs but not .NET 8.
- After .NET 8 SDK install, fixed Windows host compile errors:
  - `Application` ambiguity between WPF and WinForms.
  - `MessageBox` ambiguity between WPF and WinForms.
- Moved DPI mode configuration into the project file to remove a WinForms high-DPI manifest warning.
- Added `.gitignore` entries for generated .NET `bin/` and `obj/` directories.

### Documentation And Tidy Pass

- Updated `README.md` with:
  - Browser overlay workflow.
  - Windows WebView host workflow.
  - WSL-to-Windows launch command.
  - Updated repository structure.
  - Browser/WebView data-flow diagram.
  - `overlay_state.json` generated output.
  - Browser Source / WebView option.
  - `/state.json` sanity checks.
  - WSL localhost troubleshooting.
  - Windows WebView host build command from WSL.
  - Updated privacy/safety ignore examples.
- Updated `integrations/windows-webview-host/README.md` with:
  - Requirements.
  - Windows PowerShell and WSL workflows.
  - State-feed checks from WSL and Windows.
  - Hotkeys.
  - useful arguments.
  - publish and build commands.
  - troubleshooting notes.
- Left `integrations/obs/obs_rl_statsapi.py` type-hint/pyright changes untouched because they existed before this task chat began.

## Verification Run

- Python unit tests passed:
  - `test_imports_snapshots_and_tracks_match_updates`
  - `test_overlay_state_can_be_read_from_web_server_thread`
  - `test_parses_nested_lists_and_records`
- Python compile check passed for:
  - `listen.py`
  - `obs_rl_statsapi.py`
  - `rl_statsapi_listener/cli.py`
  - `rl_statsapi_listener/overlay_state.py`
  - `rl_statsapi_listener/web_overlay_server.py`
  - `integrations/obs/obs_rl_statsapi.py`
  - `tests/test_overlay_state.py`
  - `tools/backup_data.py`
- `git diff --check` passed.
- Windows PowerShell parse check passed for `Start-OverlayHost.ps1`.
- Windows `dotnet build` passed for `integrations/windows-webview-host/RlStatsApiOverlay.Host.csproj` with:
  - `0 Warning(s)`
  - `0 Error(s)`
- Earlier smoke checks confirmed the demo overlay server could serve `/` and `/state.json` after localhost execution was approved.

## Important Local State

- `.codex/` is ignored by git and intended as local archive context unless intentionally force-added.
- `.data/`, `.local/`, generated SQLite files, OBS output folders, `.venv/`, `bin/`, and `obj/` are ignored by git.
- Building the Windows host from WSL generated `integrations/windows-webview-host/bin/` and `integrations/windows-webview-host/obj/`; those are ignored.
- The Windows host requires the Windows .NET 8 SDK/runtime and WebView2 Runtime. Installing Linux/WSL `dotnet` does not run the WPF overlay window.
- Listener and WebView host can be split:
  - listener in WSL: `.venv/bin/python listen.py --web-overlay --obs-dir ./obs-output`
  - host as Windows process via `powershell.exe` and `Start-OverlayHost.ps1`
- If `state.json` is stale or unavailable, restart the listener and reload the host with `Ctrl+Shift+F11`.

## Suggested Commit Message From This Task Chat

```text
Add browser overlay and Windows WebView host

- serve a local HTML/CSS/JS overlay and state.json feed from the listener
- expose structured overlay state for OBS Browser Source and transparent hosts
- add a Windows WPF/WebView2 click-through overlay host with WSL launcher support
- document browser/WebView setup, feed checks, troubleshooting, and build commands
- cover web-server-thread state reads and browser overlay JSON output in tests
```
